services:
  zyracli:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    ports:
      - "8080:8080"
    entrypoint: ["/bin/bash"]